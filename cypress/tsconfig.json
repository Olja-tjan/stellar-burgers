{
  // расширяем основной конфиг всего проекта
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    // явно указываем что будем использовать типы из cypress
    // чтобы избежать пересечения с типами jest, если мы его используем
    "types": ["cypress"]
  },
  "include": [
    "../node_modules/cypress",
    "./**/*.ts"
  ]
}

// Не забыть удалить лишнее!

// вариант наставника, если возникнут ошибки

// {
//   {
//     "compilerOptions": {
//       "target": "es5",
//       "lib": ["es5", "dom"],
//       "types": ["cypress", "node"]
//     },
//     "include": ["**/*.ts"]
//   }"
// }

// Ещё можно в начале тестового файла писать следующую конструкцию:
// <reference> types="cypress"
